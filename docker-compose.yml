services:
  postgres:
    image: postgres:17-alpine
    container_name: skarb-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: skarb
      POSTGRES_USER: skarb
      POSTGRES_PASSWORD: skarb
    ports:
      - "5432:5432"
    volumes:
      - skarb_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U skarb -d skarb"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  skarb_pgdata:
